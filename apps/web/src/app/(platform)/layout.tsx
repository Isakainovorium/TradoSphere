// Platform Layout
// Main platform layout with navigation

export default function Layout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* TODO: Add Platform Layout structure */}
      {children}
    </div>
  )
}
